{
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "env": [
        "REDIS_URL",
        "BLOB_READ_WRITE_TOKEN",
        "OPENAI_API_KEY"
      ],
      "outputs": [
        ".next/**",
        "!.next/cache/**",
        ".svelte-kit/**",
        "dist/**",
        ".vercel/**"
      ]
    },
    "dev": {
      "env": [
        "REDIS_URL",
        "BLOB_READ_WRITE_TOKEN",
        "OPENAI_API_KEY"
      ]
    }
  }
}